---
name: ager-to-google-adk
description: Compile an AGER AgentGraph into Google ADK (all languages, graph and non-graph).
---

Follow the **ager-to-google-adk** skill completely. Run `scripts/emit.py`. Default is `--lang all --mode all`. Report paths.
